const obj = {
    name: "Vlad",
    age: 25,
    man: true
};

console.log(obj.name);        //в объекте через точку мы обращаемся на прямую к "name"

let arr = ['plum.png', 'orange.png', 6, {}, []];

console.log(arr[1]);        //в масиве через [] мы обращаемся на прямую к елементу 1
